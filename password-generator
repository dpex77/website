<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Password Generator Lab</title>
  <style>
    body {
      font-family: monospace, monospace;
      background: #fff;
      color: #000;
      margin: 40px;
      line-height: 1.5;
    }
    h1, h2, h3 {
      text-align: center;
      text-transform: uppercase;
    }
    pre {
      background: #f4f4f4;
      border: 1px solid #000;
      padding: 10px;
      overflow-x: auto;
    }
    code {
      font-family: monospace;
    }
    .section {
      margin-bottom: 40px;
    }
    .log {
      background: #eee;
      border-left: 4px solid #000;
      padding: 10px;
      margin: 10px 0;
    }
  </style>
</head>
<body>

<h1>Password Generator Lab</h1>

<div class="section">
<h2>Readme</h2>
<p>
This lab demonstrates a simple but secure password generator written in Python.
It uses <code>secrets</code> instead of <code>random</code> for cryptographic security, and allows flexible password lengths and character composition.
</p>
<p>
Key points:
<ul>
  <li><b>Secure randomness:</b> Uses <code>secrets</code> which is designed for cryptographic use cases.</li>
  <li><b>Character sets:</b> Includes lowercase, uppercase, digits, and punctuation symbols.</li>
  <li><b>Customizable:</b> User can choose the desired length (e.g., 5, 10, 16).</li>
  <li><b>Strong entropy:</b> Ensures unpredictable and strong passwords.</li>
</ul>
</p>
</div>

<div class="section">
<h2>Password Generator Script (password_gen.py)</h2>
<pre><code>import secrets
import string

def generate_password(length=12):
    if length &lt; 4:
        raise ValueError("Password length must be at least 4")

    # Character sets
    lower = string.ascii_lowercase
    upper = string.ascii_uppercase
    digits = string.digits
    symbols = string.punctuation

    all_chars = lower + upper + digits + symbols

    # Ensure at least one from each category
    password = [
        secrets.choice(lower),
        secrets.choice(upper),
        secrets.choice(digits),
        secrets.choice(symbols),
    ]

    # Fill the rest randomly
    password += [secrets.choice(all_chars) for _ in range(length - 4)]

    # Shuffle to avoid predictable pattern
    secrets.SystemRandom().shuffle(password)

    return ''.join(password)

if __name__ == "__main__":
    print("Generated password:", generate_password(12))
</code></pre>
</div>

<div class="section">
<h2>Sample Output</h2>
<div class="log">
<pre><code>Generated password: G8%qjW#d5!aR
Generated password: 9rF^k2X&pZmB
Generated password: tP7$LgQ1v!hD
</code></pre>
</div>
</div>

<div class="section">
<h2>Explanation of Results</h2>
<p>
Each generated password is unique, random, and includes a mix of uppercase letters, lowercase letters, digits, and symbols.
This approach prevents weak or predictable passwords.
</p>
</div>

<div class="section">
<h2>Best Practices</h2>
<ul>
  <li>Always use <code>secrets</code> for password generation in Python instead of <code>random</code>.</li>
  <li>Use at least 12–16 characters for strong passwords.</li>
  <li>Include multiple character categories to maximize entropy.</li>
  <li>Never reuse generated passwords across different systems.</li>
</ul>
</div>

</body>
</html>
